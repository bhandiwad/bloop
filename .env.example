# Copy this file to .env and fill in values
# NOTE: Do not commit real secrets to git!

# ============================================
# PRODUCTION DEPLOYMENT (Docker)
# ============================================
# For Docker deployment, use these values:

# CRITICAL: Generate secure session secret
# Run: openssl rand -hex 32
SESSION_SECRET=CHANGE_THIS_GENERATE_WITH_OPENSSL_RAND_HEX_32

# Database - Auto-configured by docker-compose
DATABASE_URL=postgresql://bloop:bloop_dev_password@postgres:5432/bloop

# Redis - Auto-configured by docker-compose (REQUIRED in production)
REDIS_URL=redis://redis:6379

# OpenAI API Key (optional - for AI-generated fake answers)
AI_INTEGRATIONS_OPENAI_API_KEY=

# Optional: OpenAI base URL (for custom endpoints)
# AI_INTEGRATIONS_OPENAI_BASE_URL=

# Server Configuration
NODE_ENV=production
PORT=5000

# ============================================
# CLOUD/NEON DEPLOYMENT (Alternative)
# ============================================
# If deploying to cloud services like Vercel/Railway/Render:
# DATABASE_URL=postgresql://<NEON_USER>:<NEON_PASSWORD>@<NEON_HOST>/<NEON_DATABASE>?sslmode=require
# REDIS_URL=redis://default:<PASSWORD>@<REDIS_HOST>:6379
